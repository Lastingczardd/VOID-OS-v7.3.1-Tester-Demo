# VOID OS Dataset Lattice

This folder contains lightweight retrieval datasets, not model weights.

- `builtin/`: generated scenario packs shipped with the app.
- `imports/`: user-imported JSONL, JSON, CSV, TXT, or Markdown datasets.

Imported material remains local. Only import data you have permission to use. Sensitive information should not be added unless you deliberately accept the privacy risk.
